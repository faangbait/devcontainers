This workspace is inside a rootless docker container running on "Red Hat Enterprise Linux 10.2 (Coughlan)".

Some of the command line tools available on the `PATH`: `rpm`, `microdnf`, `git`, `curl`, `wget`, `ssh`, `scp`, `rsync`, `gpg`, `ps`, `lsof`, `netstat`, `ncat`, `top`, `find`, `grep`, `rg`, `zip`, `unzip`, `tar`, `gzip`, `xz`, `sed`, `awk`, `jq`, `yq`, `node`, `npm`, `npx` `python`"

Use `"$BROWSER" <url>` to open a webpage in the user's browser.
