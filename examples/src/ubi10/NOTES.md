This template uses the prebuilt `faangbait/workspaces:base` image.

The image is based on Red Hat Universal Base Image 10 and includes the shared
development environment used by this repository's sandboxes.
