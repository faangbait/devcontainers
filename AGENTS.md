This repository builds sandboxes.

Agents working on this repository are running inside a sandbox built by this devcontainer.

Any changes requested are not being requested against THIS environment, but against the declarative provisioning in `src/.devcontainer*`, so that they propogate into EVERY environment.

@README.md
